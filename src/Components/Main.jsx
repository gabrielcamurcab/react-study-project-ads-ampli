function Main(props) {
    return (
        <main className="container">
            <div id="conteudo">
                <h1>Bem vindo ao {props.nome}</h1>
            </div>
        </main>
    );
}

export default Main;